{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "nginx kill",
			"command": "${workspaceFolder:nginx}/../kill_nginx.sh",
			"args": [ ],
			"options": {
				// "cwd": "${workspaceFolder:nginx}/../"
			},
			"problemMatcher": [
				"$gcc"
			],
			"presentation": {
				"revealProblems": "onProblem",
				"close": true
			}
		}
	]
}